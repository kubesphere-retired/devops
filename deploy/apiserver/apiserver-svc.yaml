apiVersion: v1
kind: Service
metadata:
  name: devops-apiserver
  annotations:
    desc: ''
  namespace: kubesphere-devops-system
  labels:
    app: devops-apiserver
spec:
  selector:
    app: devops-apiserver
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
